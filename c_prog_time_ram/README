Ο συγκεκριμένος φάκελος περιέχει τα στοιχεία της προσομοίωσης στην οποία επιλέξαμε TimingSimpleCPU και θέλαμε να εξετασουμε την αλλαγη του χρονου διεκπεραιωσης με χρηση ταχυτερης μνημης RAM(DDR3_2133_8x8) του σε σχεση με την ενναλακτικη της οποίας τα αποτελέσματα φαίνονται στην αναφορά. 
Αναφέρεται στο 3 υποερωτημα του τριτου μέρους.
Σε αυτό τον φάκελο περιέχονται τα εξής αρχεία :

$ config.dot.
$ config.ini   Περιέχει μια λίστα με κάθε SimObject που δημιουργήθηκε για την προσομοίωση και τις τιμές για τις παραμέτρους του.
$ config.json  Το ίδιο με το config.ini, αλλά σε μορφή json
$ stats.txt    Αναπαράσταση κειμένου όλων των στατιστικών στοιχείων gem5 που έχουν καταχωρηθεί για την προσομοίωση.
